import { Box, styled } from "@mui/material";

const VowelContainer = styled(Box)(() => ({
  fontFamily: "'Poppins', sans-serif",
  fontWeight: "bold",
  color: "#0068d7",
  fontSize: "28px",
  textAlign: "center",
}));

export default VowelContainer;
